const ExpenseManager = () => {
	return (
		<div className="bg-gray-100 rounded-xl border border-gray-200 max-h-[400px] overflow-hidden overflow-y-scroll flex flex-col shadow-md">
			<p className="text-2xl font-semibold px-4 pt-4">Expenses</p>
			{/* <ExpenseForm />
			{fetching ? (
				<div className="w-full">
					<p className="text-center my-10">Loading...</p>
				</div>
			) : (
				<AllExpenses />
			)} */}
		</div>
	)
}

export default ExpenseManager
